<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:DesktopLamour.Shared.Controls">

    <!-- ================================================ -->
    <!--  AppTextField base style                          -->
    <!-- ================================================ -->
    <Style TargetType="{x:Type controls:AppTextField}">
        <Setter Property="FontFamily"      Value="Segoe UI"/>
        <Setter Property="FontSize"        Value="14"/>
        <Setter Property="Foreground"      Value="{StaticResource AppColor.TextPrimary}"/>
        <Setter Property="CaretBrush"      Value="{StaticResource AppColor.TextPrimary}"/>
        <Setter Property="SelectionBrush"  Value="{StaticResource AppColor.ButtonPrimary}"/>
        <Setter Property="Background"      Value="{StaticResource AppColor.BackgroundPrimary}"/>
        <Setter Property="BorderBrush"     Value="{StaticResource AppColor.BorderRegular}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding"         Value="12,0"/>
        <Setter Property="Height"          Value="44"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:AppTextField}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"
                                          VerticalAlignment="Center"/>
                            <!-- 2026-09-13: TextDisabled (#B0B0B0) quá nhạt — đổi sang TextSecondary
                                 (#6B6B6B), vẫn đủ tối để phân biệt rõ với chữ thật (TextPrimary
                                 #1A1A1A) nhưng dễ đọc hơn hẳn. Debug bằng màu đỏ đã xác nhận build/sync
                                 hoạt động đúng — xem thêm fix TextFormattingMode ở App.xaml cho vấn đề
                                 "chữ mờ" tổng thể. -->
                            <TextBlock x:Name="PlaceholderText"
                                       Text="{TemplateBinding Placeholder}"
                                       Foreground="{StaticResource AppColor.TextSecondary}"
                                       VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource AppColor.BorderActive}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground"  Value="{StaticResource AppColor.TextDisabled}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource AppColor.BorderThin}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Text"      Value=""/>
                                <Condition Property="IsFocused" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ================================================ -->
    <!--  Error state variant                              -->
    <!-- ================================================ -->
    <Style x:Key="AppTextField.Error" TargetType="{x:Type controls:AppTextField}"
           BasedOn="{StaticResource {x:Type controls:AppTextField}}">
        <Setter Property="BorderBrush" Value="{StaticResource AppColor.BorderError}"/>
    </Style>

</ResourceDictionary>
